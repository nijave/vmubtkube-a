---
# Recommendation-only VPAs (updateMode: Off) for right-sizing insight.
# The recommender is deployed from vendored/vpa (application.vendored-vpa.yaml);
# the updater/admission-controller are intentionally not installed, so these
# never evict or mutate pods. Excluded on purpose: CNPG clusters (operator
# owns pod resources), buildkit (usage is per-build, idle-time stats mislead),
# Jobs/CronJobs, and static control-plane pods. Read recommendations with:
#   kubectl get vpa -A -o custom-columns=NS:.metadata.namespace,NAME:.metadata.name,TARGET:.status.recommendation.containerRecommendations[*].target
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-prometheus
  namespace: monitoring
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: prometheus-prom-kp-prometheus
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-thanos-receive-ingestor
  namespace: thanos
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: thanos-receive-ingestor-default
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-thanos-store
  namespace: thanos
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: thanos-store
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-thanos-compact
  namespace: thanos
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: thanos-compact
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-thanos-query
  namespace: thanos
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: thanos-query
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-thanos-receive-router
  namespace: thanos
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: thanos-receive-router
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-clickhouse-0
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: chi-hyperdx-replicated-0-0
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-clickhouse-1
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: chi-hyperdx-replicated-0-1
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-keeper-0
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: chk-hyperdx-keeper-0-0
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-keeper-1
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: chk-hyperdx-keeper-0-1
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-keeper-2
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: chk-hyperdx-keeper-0-2
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-mongo
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: hyperdx
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-hyperdx-otel-collector
  namespace: hyperdx
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hyperdx-otel-collector
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-immich-server
  namespace: immich
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: immich-server
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-immich-machine-learning
  namespace: immich
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: immich-machine-learning
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-jellyfin
  namespace: media
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: jellyfin
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-radarr
  namespace: media
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: radarr
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-sonarr
  namespace: media
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sonarr
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-prowlarr
  namespace: media
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prowlarr
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-sabnzbd
  namespace: media
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sabnzbd
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-searxng
  namespace: default
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: searxng
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-reloader
  namespace: default
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: reloader-reloader
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-woodpecker-server
  namespace: woodpecker
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: woodpecker-server
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-woodpecker-agent
  namespace: woodpecker
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: woodpecker-agent
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-otel-collector
  namespace: monitoring
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: otel-collector-opentelemetry-collector
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-otel-logs
  namespace: monitoring
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: DaemonSet
    name: otel-logs-opentelemetry-collector-agent
  updatePolicy:
    updateMode: "Off"
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-otel-collector-contour
  namespace: projectcontour
  annotations:
    # The VPA CRD is installed by the vpa Application in this same repo; skip
    # the dry-run so a cold bootstrap doesn't fail the root app sync before
    # the CRD exists (retries apply these once it does).
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: otel-collector
  updatePolicy:
    updateMode: "Off"
