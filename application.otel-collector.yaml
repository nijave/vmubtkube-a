apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: otel-collector
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: "https://kubernetes.default.svc"
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - PruneLast=true
    - ServerSideApply=true
    - ApplyOutOfSyncOnly=true
  source:
    chart: opentelemetry-collector
    repoURL: https://open-telemetry.github.io/opentelemetry-helm-charts
    targetRevision: 0.152.1
    helm:
      releaseName: otel-collector
      valuesObject:
        mode: deployment
        image:
          repository: docker.io/otel/opentelemetry-collector-contrib
        enableConfigChecksumAnnotation: false

        presets:
          kubernetesAttributes:
            enabled: true

        config:
          processors:
            batch:
              send_batch_size: 1024
              timeout: 5s
            memory_limiter:
              check_interval: 5s
              limit_percentage: 80
              spike_limit_percentage: 25
            tail_sampling:
              decision_wait: 300s
              policies:
              - name: error
                type: status_code
                status_code: {status_codes: [ERROR]}
              - name: random
                type: probabilistic
                probabilistic: {sampling_percentage: 5}
            # Host log shippers (fluent-bit on cluster nodes, plus external hosts
            # forwarding to otel-collector.k8s.somemissing.info) tag records with
            # log.source=journald|syslog and put service.name / host.name in log
            # attributes. Promote those to OTel resource attributes so HyperDX
            # populates ServiceName and ResourceAttributes.
            transform/promote_host_resource_attrs:
              error_mode: ignore
              log_statements:
                - context: log
                  conditions:
                    - attributes["log.source"] == "journald" or attributes["log.source"] == "syslog"
                  statements:
                    - set(resource.attributes["service.name"], attributes["service.name"]) where attributes["service.name"] != nil
                    - set(resource.attributes["host.name"],    attributes["host.name"])    where attributes["host.name"]    != nil
                    - set(resource.attributes["log.source"],   attributes["log.source"])
                    - delete_key(attributes, "service.name")
                    - delete_key(attributes, "host.name")
                    - delete_key(attributes, "log.source")

          receivers:
            jaeger: null
            otlp:
              protocols:
                grpc:
                  endpoint: ${env:MY_POD_IP}:4317
                  tls:
                    cert_file: /tls/tls.crt
                    key_file:  /tls/tls.key
                http:
                  endpoint: ${env:MY_POD_IP}:4318
                  tls:
                    cert_file: /tls/tls.crt
                    key_file:  /tls/tls.key
            prometheus: null
            zipkin: null

            # Syslog ingest. tcplog/udplog (not the syslog receiver) so we can
            # accept arbitrary line-based formats — RFC 5424, RFC 3164, the
            # bracketed-timestamp Ubiquiti format, plus a passthrough for
            # anything else. add_attributes populates net.peer.ip/port so we
            # always have the sender's identity even when the payload doesn't
            # include a hostname.
            udp_log/syslog:
              listen_address: 0.0.0.0:5140
              add_attributes: true
              operators: &syslog_ops
                # RFC 5424: <PRI>VERSION TIMESTAMP HOST APP PROC MSGID SD MSG
                - type: regex_parser
                  if: 'body matches "^<\\d+>\\d\\s"'
                  regex: '^<(?P<pri>\d+)>(?P<version>\d)\s+(?P<time>\S+)\s+(?P<host>\S+)\s+(?P<app>\S+)\s+(?P<proc>\S+)\s+(?P<msgid>\S+)\s+(?P<sd>\[[^\]]*\]|-)\s*(?P<message>.*)$'
                  parse_from: body
                  parse_to: attributes
                  on_error: send
                # RFC 3164: <PRI>MMM dd HH:MM:SS HOST IDENT[PID]: MSG
                - type: regex_parser
                  if: 'body matches "^<\\d+>[A-Za-z]"'
                  regex: '^<(?P<pri>\d+)>(?P<time>\S+\s+\S+\s+\S+)\s+(?P<host>\S+)\s+(?P<ident>[\w\-./]+)(?:\[(?P<pid>\d+)\])?(?:[^:]*:)?\s*(?P<message>.*)$'
                  parse_from: body
                  parse_to: attributes
                  on_error: send
                # Ubiquiti / EdgeOS: [unix.fractional_ts] payload
                - type: regex_parser
                  if: 'body matches "^\\["'
                  regex: '^\[(?P<time>\d+(?:\.\d+)?)\]\s*(?P<message>.*)$'
                  parse_from: body
                  parse_to: attributes
                  on_error: send
                # Map captured fields to OTel semantic conventions.
                - type: move
                  if: 'attributes["host"] != nil'
                  from: attributes["host"]
                  to: attributes["host.name"]
                - type: move
                  if: 'attributes["app"] != nil'
                  from: attributes["app"]
                  to: attributes["service.name"]
                - type: move
                  if: 'attributes["ident"] != nil'
                  from: attributes["ident"]
                  to: attributes["service.name"]
                # Tag for transform/promote_host_resource_attrs.
                - type: add
                  field: attributes["log.source"]
                  value: syslog

            tcp_log/syslog:
              listen_address: 0.0.0.0:5140
              add_attributes: true
              operators: *syslog_ops

          exporters:
            debug: {}
            # otlp_grpc:
              # endpoint: jaeger-collector.monitoring:4317
              # tls:
                # insecure: true
            otlp_grpc:
              endpoint: hyperdx-otel-collector.hyperdx:4317
              compression: gzip
              headers:
                authorization: 67591567-eca7-43b1-b218-4af3992e5e9b
              tls:
                insecure: true

          service:
            pipelines:
              logs:
                receivers: [otlp]
                processors: [memory_limiter, transform/promote_host_resource_attrs, batch]
                exporters: [otlp_grpc]
              logs/syslog:
                receivers: [udp_log/syslog, tcp_log/syslog]
                processors: [memory_limiter, transform/promote_host_resource_attrs, batch]
                exporters: [otlp_grpc]
              metrics: null
              traces:
                receivers: [otlp]
                processors: [memory_limiter, tail_sampling, batch]
                exporters:
                  # - debug
                  - otlp_grpc
                  # - otlphttp

        ports:
          jaeger-compact:
            enabled: false
          jaeger-thrift:
            enabled: false
          jaeger-grpc:
            enabled: false
          zipkin:
            enabled: false
          metrics:
            enabled: true
          syslog-udp:
            enabled: true
            containerPort: 5140
            servicePort: 514
            protocol: UDP
          syslog-tcp:
            enabled: true
            containerPort: 5140
            servicePort: 514
            protocol: TCP

        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 10m
            memory: 128Mi

        podAnnotations:
          # Restart when the TLS secret rotates (cert-manager renews ~every 60d).
          secret.reloader.stakater.com/reload: otel-collector-tls

        extraVolumes:
          - name: tls
            secret:
              secretName: otel-collector-tls
        extraVolumeMounts:
          - name: tls
            mountPath: /tls
            readOnly: true

        rollout:
          rollingUpdate:
            maxUnavailable: 0
            maxSurge: 100%

        service:
          annotations:
            external-dns.alpha.kubernetes.io/hostname: otel-collector.k8s.somemissing.info,syslog.k8s.somemissing.info

        serviceMonitor:
          enabled: true
          extraLabels:
            release: prom

        prometheusRule:
          enabled: true
          defaultRules:
            enabled: true
          extraLabels:
            release: prom
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: otel-collector
  namespace: monitoring
spec:
  secretName: otel-collector-tls
  commonName: otel-collector.k8s.somemissing.info
  dnsNames:
  - otel-collector.k8s.somemissing.info
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-webhook-dnsimple-production
